import React from "react";
import styled from "styled-components";

const Header = () => {
  const test = "";

  return (
    <div>
      <div>Header</div>
    </div>
  );
};

export default Header;
