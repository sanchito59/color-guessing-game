import React from "react";
import styled from "styled-components";

const MenuBar = () => {
  const test = "";

  return (
    <div>
      <div>MenuBar</div>
    </div>
  );
};

export default MenuBar;
